apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "vaultfs.fullname" . }}-config
  labels:
    {{- include "vaultfs.labels" . | nindent 4 }}
data:
  config.toml: |
    {{- .Values.config | nindent 4 }}
